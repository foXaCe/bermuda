# Overhaul state — bermuda (ha-integration-overhaul)

> Fichier de suivi durable — supprimé au commit final Phase 9.

## Contexte
- Branche : `refactor/integration-overhaul-2026-07-02` (base `main` @ edf27fa)
- Tag de retour : `backup/before-overhaul-20260702-131634`
- HA dev : container `homeassistant` 2026.7.0, path `/home/stephane/homeassistant/config/`
- venv test : Python 3.13.11, homeassistant 2026.2.3, pytest-homeassistant-custom-component 0.13.316

## Phases
- [x] Phase 0 — Préflight (branche + tag + sync ; branche bootstrap squash-mergée en main via PR #73, supprimée localement)
- [x] Phase 0.5 — État de l'art web (agent Sonnet) → scratchpad/ha-backend-state-of-art-20260702.md (résumé ci-dessous)
- [x] Phase 1 — Découverte (agent Sonnet) + inventaire UID + baseline perf
- [x] Phase 2 — Architecture : `config_entry=entry` explicite au super().__init__() du coordinator (dépréciation 2026.8), guard mort retiré, `async_config_entry_first_refresh()` dans __init__.py, `reload_on_update=False` dans config_flow (conformité 2026.12). 460 tests OK, deploy HA dev 0 erreur.
- [x] Phase 3 — Refactor structurel ciblé : 7 clés mortes supprimées d'options_text.py (×2 langues, dont calibration_row_rssi non vue par l'agent), commentaires orphelins coordinator.py retirés (handle_state_changes, self.updaters), code blueprint commenté purgé de config_flow.py. Décision : repairs inline conservé (is_fixable=False → repairs.py non requis), pas de re-split. 460 tests OK, deploy 0 erreur.
- [x] Phase 4 — Client externe (bluetooth) : audit sans modification. async_current_scanners (API publique 2025.2+) ✓, _get_manager privé isolé+guardé ✓, load_manufacturer_ids dégradation gracieuse ✓, IRK jamais loggés en clair ✓, branchement bleak conservé (requis tant que min HA 2025.3 < 2025.8, instruction de suppression en place). Pas de deploy (aucun changement de code).
- [x] Phase 5 — Entités : CoordinatorEntity[BermudaDataUpdateCoordinator] générique (import runtime sûr, pas de cycle), self.coordinator redondants supprimés, DeviceInfo typé (dr.DeviceInfo) sur les 2 device_info. unique_id inchangés (décision). has_entity_name déjà présent sur les 5 plateformes. 460 tests OK, deploy 0 erreur.
- [x] Phase 6 — Config flow & translations : audit sans modification. Parité stricte strings/en/fr (202 clés identiques, 0 divergence placeholders, strings==en), style FR vérifié (vouvoiement uniforme, 0 anglicisme, typographie FR), 12 services cohérents code/services.yaml/strings. el/nl/pt/nb incomplètes → hors périmètre, au rapport. Pas de deploy (aucun changement).
- [ ] Phase 7 — Qualité transverse
- [ ] Phase 8 — Tests (cible ≥ 95 %)
- [ ] Phase 9 — Validation finale

## Baseline perf (Phase 1.5.bis — 2026-07-02)
- `Setup of domain bermuda took 0.00 seconds` (HA dev 2026.7.0, 13:14:02)
- importtime .venv : `custom_components.bermuda` cumulé 959 667 µs (dont HA core ≈ 446 ms via bluetooth ; part bermuda ≈ 513 ms) ; `coordinator` cumulé 459 520 µs
- Config entry dev : v2.1, options `{"configured_devices": [], "exclude_devices": [], "track_categories": ["apple"]}`

## Inventaire unique_id (Phase 1.5 — registry HA dev, 4 entités)
```
sensor.bermuda_global_nombre_d_appareils_visibles   BERMUDA_GLOBAL_VISIBLE_DEVICE_COUNT
sensor.bermuda_global_nombre_de_proxys_actifs       BERMUDA_GLOBAL_ACTIVE_PROXY_COUNT
sensor.bermuda_global_nombre_total_d_appareils      BERMUDA_GLOBAL_DEVICE_COUNT
sensor.bermuda_global_nombre_total_de_proxys        BERMUDA_GLOBAL_PROXY_COUNT
```
Patterns code : tout dérive de `device.unique_id` (MAC/IRK/uuid_major_minor) + suffixes `_floor`, `_scanner`, `_rssi`, `_range`, `_{scanner}_range[_raw]`, `_area_switch_reason`, `_area_last_seen`, `_micro_location`, `_in100_*`, `_ref_power`, `_mobility` ; globaux fixes `BERMUDA_GLOBAL_*`. Test de régression : `tests/test_unique_id_regression.py`.

## État de l'art (Phase 0.5 — points actionnables)
1. 🔴 2026.8 : `config_entry=entry` obligatoire dans `super().__init__()` du DataUpdateCoordinator (coordinator.py ~L124-171)
2. 🟡 2026.12 : interdiction listener + reload methods combinés dans les flows (déprécié 2026.6) — à vérifier
3. Adopter `async_config_entry_first_refresh()` (remplace refresh manuel + vérif dans `__init__.py`)
4. OptionsFlow / runtime_data / forward_entry_setups / manifest : déjà conformes
5. Bluetooth : callback global en ACTIVE explicite = non affecté par défaut AUTO 2026.6 ; `_hascanners: set[BaseHaScanner]` = interface habluetooth non garantie stable (vigilance, pas un bug)
6. Opportunité 2027.7 (non urgente) : `BaseScannerEntity`/`in_zones`/`tracking_type` pour device_tracker
7. Quality Scale : règle `test-coverage` = > 95 %

## Dette identifiée (Phase 1)
- pytest.ini écrase [tool.pytest.ini_options] → coverage gate (fail_under=100) inactive partout, CI sans --cov
- Coverage réel 93,06 % (460 tests) ; faibles : subentry_flow 79 %, bermuda_device_scanner 80 %, pruning 82 %, __init__ 86 %
- options_text.py : 6 clés `_DESCRIPTION_TEXTS` mortes (×2 langues)
- coordinator.py : 2 commentaires orphelins (L212 handle_state_changes, L280 self.updaters)
- strict-typing todo (ANN* désactivés dans pyproject) — Platinum
- Traductions el/nl/pt/nb incomplètes (communautaires — hors périmètre, à noter au rapport)
- repairs inline dans coordinator_scanners.py (pattern acceptable si issue non-fixable — à trancher en Phase 3)

## Décisions validées
- AUCUN changement de format unique_id (intégration shippée, schéma cohérent, test de régression en place)
- Pas de re-split structurel massif : le code est déjà modulaire (mixins) ; Phase 3 = nettoyage ciblé uniquement
- Domain, features : inchangés
- Cible coverage : ≥ 95 % (règle HA test-coverage) avec gate réactivée

## TODO restants
- (rempli au fil des phases)
